[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "DATABASE_URL=$DATABASE_URL npx prisma migrate deploy && node dist/src/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
